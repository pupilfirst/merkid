<!DOCTYPE html>
<html>
<head>
  <title>CoronaSafe Engineering Fellowship Application</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Global site tag (gtag.js) - Google Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-VEZME979JN"></script>
  <script>
    window.dataLayer = window.dataLayer || [];

    function gtag() {
      dataLayer.push(arguments);
    }

    gtag('js', new Date());

    gtag('config', 'G-VEZME979JN');
  </script>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class="bg-gray-800">

<header class="max-w-3xl mx-auto py-4 lg:py-8 px-4 xl:px-0">
  <div class="flex justify-between items-center">
    <%= image_tag('logo-dark.svg', class: 'bock w-36 md:w-44') %>
    <%= image_tag('aicte-logo.png', class: 'block w-20 md:w-24') %>
  </div>
</header>


<div class='<%= @is_admin_page ? "max-w-5xl mx-auto" : "max-w-3xl mx-auto py-4 lg:py-8 px-4 xl:px-0" %>'>
  <% if @student %>
    <div class="flex justify-between">
      <% if (current_page?(root_path) || current_page?(students_path)) %>
        <div></div>
      <% else %>
        <p class="mb-0 subdued">
          <%= link_to "Home", root_path, class: "subdued" %>
        </p>
      <% end %>
      <div class="flex md:mb-10 mb-4">
        <p class="mb-0 mr-32 subdued"> <%= @student.more_context_name %> </p>
        <p class="mb-0 subdued"> <%= link_to "Logout", logout_url, class: "subdued" %></p>
      </div>
    </div>
  <% end %>

  <% if @is_admin_page %>
    <h1 class="text-primary-700">Admin</h1>
  <% else %>
    <h1 class="hidden" style="margin-bottom: 8px; padding-bottom: 0">CoronaSafe Engineering Fellowship</h1>
    <h2 class="text-4xl text-white mb-10">
      <%= @stats_page ? 'Stats' : 'Application Steps' %>
    </h2>
  <% end %>

  <%= render partial: "layouts/flash", locals: { msg: flash[:info], css: "flash animation-vibrate bg-primary-500 text-black font-bold rounded shadow" } %>
  <%= render partial: "layouts/flash", locals: { msg: flash[:success], css: "flash animation-vibrate bg-primary-500 text-black font-bold rounded shadow" } %>
  <%= render partial: "layouts/flash", locals: { msg: flash[:error], css: "flash animation-vibrate bg-red-500 text-black font-bold rounded shadow" } %>

  <%= yield %>

  <div class="mt-32 text-white">
    <div>Please note that it is required that you read and agree to the terms and conditions before applying for the
      program. <%= link_to "Click here to read", terms_path, class: 'font-bold' %>.
    </div>
    <div class="mt-4">For any questions, email us at
      <a class="font-bold" href="mailto:fullstack@pupilfirst.org">fullstack@pupilfirst.org</a>.
    </div>
  </div>

  <% if @student %>
    <div class="exposed-ceiling">
<pre class="">
<%== JSON.pretty_generate(@student.as_json) %>
</pre>
      For your reference.
    </div>
  <% end %>
</div>
</body>
</html>
